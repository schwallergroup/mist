Modify the input and output paths, and add the API key in the script deepseek_traces.py
Use the same format for the input prompt file as shown in train.txt. Note the "Answer: " keyword, which will be used to extract the answer for further processing
